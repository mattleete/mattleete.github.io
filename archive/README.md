# Archive

Superseded / historical work, kept for reference. **Nothing here is served** (it lives outside `docs/`), and nothing here should be linked from the live site.

If you're looking for the current site, it's in `docs/`.

## Contents

- **`superseded-site/`** — the original early draft of the portfolio (`index.html`, `style.css`, `script.js`, `SITE-GUIDE.md`), fully replaced by the current design-system build in `docs/`.
- **`docs-drafts/`** — old pages pulled out of `docs/` so they'd stop being publicly reachable:
  - `portfolio-home.html` — an early home-page draft, superseded by `docs/index.html`.
  - `portfolio-case-study.html` — an old generic case-study template, superseded by the real case studies.
  - `icon-preview.html` — a throwaway icon-preview scratch page.

To retire something in future: move it here (not into `docs/`), add a line above, and commit.
