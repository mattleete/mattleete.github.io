---
name: portfolio-design
description: Design system, taste rules, and layout patterns for Matt Leete's portfolio (mattleete.github.io). Load this BEFORE creating or editing any page, component, or style in the Portfolio repo, so the work stays consistent and on-brand.
---

# Portfolio Design System

Single source of truth for building and editing pages on Matt Leete's portfolio. **Read this before touching any HTML/CSS in this repo.**

## Golden rules (non-negotiable)

1. **Never hardcode colours, sizes, or spacing.** Use the CSS custom properties from `src/assets/design-system.css`. If you're typing a hex code or a raw px value that isn't a one-off layout number, stop — there's a token for it.
2. **Every page gets its stylesheets from its layout** (`stylesheets:` front matter in `layouts/*.njk`). Page-specific CSS lives in `src/assets/<page>.css`, never inline, and holds only rules unique to that page.
3. **Dark mode is first-class.** The theme is driven by `data-theme="light|dark"` on `<html>`. Never introduce a colour that only works in one theme. Test both.
4. **Responsive is required.** Every page must work at 768px and below. The shared breakpoint is `@media (max-width: 768px)`.
5. **One accent colour only:** electric blue `var(--accent)` (`#1a56ff`). Do not add second/third accent hues. Colour comes from the signature background animation, not from UI chrome.

## Aesthetic — "minimal / editorial"

Monochrome greyscale + a single electric-blue accent. Instrument Sans throughout. Generous whitespace. Motion is restrained and purposeful — the signature background (light mesh gradient / dark aurora) and the hero ring are the personality; everything else stays calm.

**Do:** large confident type, lots of breathing room, thin `1px` borders in `--border-default`, sentence case in body copy, uppercase only for labels/nav/tags, subtle hover transitions (~0.2s).

**Don't:** add drop shadows for depth (this system uses borders + whitespace, not shadows), introduce new fonts, use more than the one accent, add bouncy/heavy animation, or use pure `#000`/`#fff` directly instead of tokens.

## Tokens (defined in `src/assets/design-system.css`)

**Colour** — raw palette `--black --grey-1..6 --white --accent --accent-hover --bg`; semantic aliases (prefer these) `--text-primary --text-secondary --text-tertiary --text-disabled --surface-page --surface-subtle --border-default --border-strong`. In dark mode these invert automatically — use the vars and it just works.

**Type scale** — `--size-display:72 --size-h1:40 --size-h2:24 --size-h3:16 --size-body:14 --size-small:12 --size-label:11 --size-micro:10` (px). Utility classes exist: `.t-display .t-h1 .t-h2 .t-h3 .t-body .t-small .t-label .t-micro`. Font weights used: 400 / 500 / 600.

**Space scale** — `--space-1:4 --space-2:8 --space-3:16 --space-4:32 --space-5:64 --space-6:128 --space-7:224` (px). Section padding is `128px 64px` desktop / `64px 24px` mobile.

**Layout** — `--max-width:1440px` (site wrapper), `--content-width:1312px` (section inner), `--radius-pill:9999px`.

## Component classes (already in the shared CSS — reuse, don't reinvent)

- `.site-wrapper` — max-width 1440 centred; wrap page content in it.
- `nav` + `.nav-logo` `.nav-links` `.nav-link` `.nav-hamburger` — the header. Includes the mobile hamburger + `.theme-toggle`/`.theme-btn` with sun/moon icons.
- `.wave-divider` — thick wavy section divider. Inverts whatever is behind it (`mix-blend-mode: difference`), so it needs no theme colours and works in light + dark automatically. Tune via `--wave-height/-period/-duration/-opacity`; line thickness is the SVG `stroke-width` in the CSS. Replaced the old scrolling skills marquee.
- `.section` + `.section-inner` `.section-title` `.cards-row` — a content section.
- `.card` + `.card-image` `.card-title` `.card-desc` `.card-divider` `.card-bottom` `.card-tags` `.tag` `.card-arrow` — the work/fun/about cards (432px wide, horizontal-scroll on mobile).
- `footer` + `.footer-left` `.footer-static` `.footer-arrows-*` `.footer-links` `.footer-link` `.footer-mobile-label` — the footer.
- `.btn` / `.btn-secondary` — pill buttons.
- Background: `.mesh` (+ `.blob-1..5`, light) and `.aurora` (+ `.aurora-inner`, dark). Include both blocks near the top of `<body>` with `aria-hidden="true"`.

## How pages are built (Eleventy + Decap CMS — since 2026-09-18)

The site is generated by **Eleventy** from `src/` into `_site/` and deployed by GitHub Actions. Copy lives in content files; markup lives in layouts. **Do not hand-write a full HTML page** — add or edit content, and touch a layout only when the *structure* changes.

| Page | Content file (what editors change) | Layout (structure) | Page CSS |
|---|---|---|---|
| Home | `src/_data/home.yml` (hero words, Work/Fun/About cards) | `src/index.njk` | `assets/home.css` + `home.js` |
| Case studies | `src/case-studies/<slug>.md` — front matter + Markdown body | `layouts/case-study.njk` | `assets/case-study.css` |
| About / CV / Contact | `src/about.md`, `cv.md`, `contact.md` — front matter (+ bio body) | `layouts/{about,cv,contact}.njk` | `assets/{about,cv,contact}.css` |
| Everything | `src/_data/site.yml` (name, email, nav, password) | `layouts/base.njk` (head, OG tags, nav, footer, theme.js) | — |

- **New case study:** create `src/case-studies/<slug>.md` (or use `/admin/`). Front matter: `title, subtitle, description, breadcrumb, tags, meta[{label,value}], overview{challenge,approach,outcome}, hero_image, gated, draft, next`. Body is plain Markdown: each `## Heading` becomes a numbered section; a paragraph that is only **bold** becomes a pull-quote; a bare image becomes a full-width figure. `draft: true` = never built. The URL is `portfolio-case-study-<slug>.html`.
- **New page type:** add `layouts/<name>.njk` with front matter `layout: layouts/base.njk` and `stylesheets: [assets/design-system.css, assets/<name>.css]`; put its CSS in `assets/<name>.css`; give the content file `layout` + `permalink`. Add its fields to `src/admin/config.yml` so it is CMS-editable.
- **Case-study layout uses only `case-study.css`** (not `design-system.css`) — its nav is not blend-moded and the two stylesheets conflict. Keep it that way until they are reconciled.
- **The CMS** is Decap at `/admin/` (`src/admin/config.yml`); editorial workflow, so Save = draft PR, Publish = live. Every field an editor sees is declared there — when you add a front-matter field, add it to the config too.
- **Preview:** `npm run serve` → http://localhost:8080/. Never edit or commit `_site/`.

## Page-build / edit checklist

- [ ] Content is in a content file, structure in a layout; no hand-written full pages. Page CSS in `assets/<page>.css`, not inline.
- [ ] No hardcoded hex/px where a token exists.
- [ ] Nav + footer come from `base.njk` (never copied into a page).
- [ ] Works in light AND dark (`data-theme` toggled).
- [ ] Works at ≤768px (nav collapses to hamburger, cards scroll, type scales down).
- [ ] `title` + `description` in front matter (base.njk writes `<title>`, meta and OG tags); images have `alt`.
- [ ] Verified in a real browser (`npm run serve`), both themes, before declaring done. New CMS fields added to `src/admin/config.yml`.

## Case study pages

Case studies are **password-gated by default** (`gated: true`; the password is `case_study_password` in `src/_data/site.yml` — a courtesy gate, readable in the source). Reference implementation: `src/case-studies/occypicks.md` (public, with real media) and `layouts/case-study.njk`. Structure: hook/outcome → context & role → problem → process/decisions → outcome with metrics → reflection. Lead with impact and numbers.

## Pointers

- Tokens (source of truth): `design-source/tokens.json`
- Shared CSS: `src/assets/design-system.css` (+ `case-study.css` for case studies)
- Reference layout: `src/_includes/layouts/base.njk`; reference page: `src/index.njk` + `src/_data/home.yml`
- Reference case study: `src/case-studies/occypicks.md` + `layouts/case-study.njk`
- CMS config: `src/admin/config.yml`
- Overall plan + locked decisions: `PLAN.md` (repo root)
